name: fuel_sales_analysis
description: "A new Flutter project"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2  # Bu varsayılan olarak var
  xml: ^6.5.0  # XML dosyasını okumak için
  fl_chart: ^0.66.2  # Güzel grafikler için
  file_picker: ^6.1.1  # Web'de dosya seçimi için 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/Sale.xml 